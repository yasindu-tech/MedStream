name: Azure Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  placeholder-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Azure deployment placeholder
        run: echo "Add Azure build and deployment steps here."
